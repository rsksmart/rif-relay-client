import { expect } from "chai"

describe('CachedExchangeApi', function() {
    it('should perform a call to the inner exchange if the value is not cached', function () {
        expect(false);
    })

    it('should perform a call to the inner exchange if the cached value is expired', function () {
        expect(false);
    })

    it('should not perform a call to the inner exchange if the cached value is not expired', function () {
        expect(false);
    })

    it('should store the value after calling the the inner exchange', function () {
        expect(false);
    })
})