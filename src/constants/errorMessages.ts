import { ENVELOPING_ROOT } from './configs';

const MISSING_CONFIG_ROOT = `Could not read enveloping configuration. Make sure your configuration is nested under ${ENVELOPING_ROOT} key.`;

export { MISSING_CONFIG_ROOT };
